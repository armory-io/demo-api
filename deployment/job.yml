apiVersion: batch/v1
kind: Job
metadata:
  name: jobjob-v000
  namespace: ethan-dev
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: test
          image: nginx:alpine
          command:
          -  echo
          - "hello world"
